const formatElmFiles = require('./format-elm-files');
const generate = require('./generate');
const sourceApi = require('./source-api');

module.exports = {
  formatElmFiles,
  generate,
  sourceApi,
};
